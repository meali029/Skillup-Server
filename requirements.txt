# Python dependencies for CNIC OCR extraction.
# The backend runs src/scripts/paddle_ocr.py, which currently uses EasyOCR.

# OCR and image processing
easyocr==1.7.2
opencv-python-headless==4.9.0.80
numpy>=1.21.0,<2.0.0
