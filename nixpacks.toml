[phases.setup]
nixPkgs = ['nodejs_20', 'python311', 'python311Packages.pip']

[phases.install]
cmds = [
  'npm ci',
  'python3 -m pip install --upgrade pip',
  'python3 -m pip install -r requirements.txt'
]

[phases.build]
cmds = ['echo "skip build"']

[start]
cmd = 'npm start'
